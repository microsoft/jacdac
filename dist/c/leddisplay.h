// Autogenerated C header file for LED Display
#ifndef _JACDAC_SPEC_LED_DISPLAY_H
#define _JACDAC_SPEC_LED_DISPLAY_H 1

#define JD_SERVICE_CLASS_LED_DISPLAY  0x1609d4f0
#define JD_LED_DISPLAY_MAX_PIXELS_LENGTH 64

// enum LightType (uint8_t)
#define JD_LED_DISPLAY_LIGHT_TYPE_WS2812B_GRB 0x0
#define JD_LED_DISPLAY_LIGHT_TYPE_APA102 0x10
#define JD_LED_DISPLAY_LIGHT_TYPE_SK9822 0x11

// enum Variant (uint8_t)
#define JD_LED_DISPLAY_VARIANT_STRIP 0x1
#define JD_LED_DISPLAY_VARIANT_RING 0x2
#define JD_LED_DISPLAY_VARIANT_STICK 0x3
#define JD_LED_DISPLAY_VARIANT_JEWEL 0x4
#define JD_LED_DISPLAY_VARIANT_MATRIX 0x5

/**
 * Read-write bytes. A buffer of 24bit RGB color entries for each LED, in R, G, B order.
 */
#define JD_LED_DISPLAY_REG_PIXELS JD_REG_VALUE

/**
 * Read-write ratio u0.8 (uint8_t). Set the luminosity of the strip.
 * At `0` the power to the strip is completely shut down.
 */
#define JD_LED_DISPLAY_REG_BRIGHTNESS JD_REG_INTENSITY

/**
 * Read-only ratio u0.8 (uint8_t). This is the luminosity actually applied to the strip.
 * May be lower than `brightness` if power-limited by the `max_power` register.
 * It will rise slowly (few seconds) back to `brightness` is limits are no longer required.
 */
#define JD_LED_DISPLAY_REG_ACTUAL_BRIGHTNESS 0x180

/**
 * Constant LightType (uint8_t). Specifies the type of light strip connected to controller.
 */
#define JD_LED_DISPLAY_REG_LIGHT_TYPE 0x181

/**
 * Constant # uint16_t. Specifies the number of pixels in the strip.
 */
#define JD_LED_DISPLAY_REG_NUM_PIXELS 0x182

/**
 * Constant # uint16_t. If the LED pixel strip is a matrix, specifies the number of columns.
 */
#define JD_LED_DISPLAY_REG_NUM_COLUMNS 0x183

/**
 * Read-write mA uint16_t. Limit the power drawn by the light-strip (and controller).
 */
#define JD_LED_DISPLAY_REG_MAX_POWER JD_REG_MAX_POWER

/**
 * Constant Variant (uint8_t). Specifies the shape of the light strip.
 */
#define JD_LED_DISPLAY_REG_VARIANT JD_REG_VARIANT

#endif
