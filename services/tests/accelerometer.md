# Accelerometer tests

## tilt events

Tilt the device up

    awaitEvent(tilt_up, true)

Now, tilt it down

    awaitEvent(tilt_down, true)

Now, tilt it left

    awaitEvent(tilt_left, true)

Now, tilt it right

    awaitEvent(tilt_right, true)
